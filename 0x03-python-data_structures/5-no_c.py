#!/usr/bin/python3
def no_c(my_string):
    str_list = my_string.split()
    str_len = len(str_list)
    new_str = []
    for i in str_list:
        if i != 'c' or i != 'C':
            new_str = append.(i)
    return new_str.join()
